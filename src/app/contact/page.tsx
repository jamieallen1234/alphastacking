import ContactPage from '@/components/ContactPage'

export const metadata = {
  title: 'Contact & feedback — Alpha Stacking',
  description:
    'Send site feedback, feature requests, or corrections. Optional email for replies. For educational use only; not investment advice.',
}

export default function UsContactPage() {
  return <ContactPage />
}
